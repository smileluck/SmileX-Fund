# 替换总价值和盈亏字段为持仓金额和持仓盈亏

## 问题分析
根据用户需求，需要将代码中使用的 `totalValue`（总价值）和 `profit`（盈亏）字段替换为 `holdingAmount`（持仓金额）和 `holdingProfit`（持仓盈亏）字段。

## 修改计划

### 1. 修改类型定义
- **文件**: `lib/mockData.ts`
- **修改**: 更新 `UserHolding` 接口，移除冗余字段，确保字段命名一致

### 2. 修改数据计算逻辑
- **文件**: `app/page.tsx`
- **修改**: 
  - 更新 `calculateHoldingData` 函数，使用 `holdingAmount` 和 `holdingProfit`
  - 修改 `handleAddHolding` 函数中的数据处理逻辑
  - 修改 `handleBatchAddHolding` 函数中的数据处理逻辑
  - 修改 `handleEditHolding` 函数中的数据处理逻辑

### 3. 修改持仓数据处理
- **文件**: `app/components/FundTab.tsx`
- **修改**: 
  - 更新 `currentWalletHoldings` 的计算逻辑
  - 修改持仓列表的渲染和计算逻辑
  - 确保所有使用 `totalValue` 和 `profit` 的地方都改为使用 `holdingAmount` 和 `holdingProfit`

### 4. 检查其他组件
- **文件**: `app/components/AddHoldingModal.tsx` 和 `app/components/BatchAddHoldingModal.tsx`
- **修改**: 确保这些组件中的字段引用与修改后的命名一致

## 技术实现要点
1. 保持数据结构的一致性，避免冗余字段
2. 确保所有计算逻辑（如盈亏率计算）使用正确的字段
3. 保持代码风格和命名规范的一致性
4. 确保修改后的数据流转和渲染逻辑正常工作

## 验证步骤
1. 检查所有修改后的文件是否有语法错误
2. 运行项目确保功能正常
3. 验证持仓数据的计算和显示是否正确
4. 验证添加、编辑和批量操作是否正常工作